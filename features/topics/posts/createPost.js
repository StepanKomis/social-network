const dbConnection = require('../../database/connection');
const express = require('express');

function createPost (name, text, media,){
    console.log('workin');    
};

module.exports = createPost();